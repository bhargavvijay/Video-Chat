const form=document.getElementById('view-transcripts-form');

form.addEventListener('submit',(e)=>{
    e.preventDefault();
    if(form[meeting-id].value)
    {
        
    }
    else{
        alert('Enter meeting id');
    }
})